import { Box, styled } from '@mui/material'

export const TopBar = styled(Box)(({ theme }) => ({
  backgroundColor: '#fff',
  padding: '20px 30px',
  display: 'flex',
  alignItems: 'flex-end',
  flexDirection: 'column'
}))
